import { PreloadedState } from "../interface/preloadedState";

export const preloadedState: PreloadedState = {
    article: {
        articleIndex: -1,
        articles: [],
        pageNumber: 1,
        totalResults: 0
    },
    loading: 0,
};
